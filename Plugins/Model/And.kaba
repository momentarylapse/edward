use edward

void main()

	let a = new Geometry.Cube(vector(-50, -50, -10), vector.EX * 100, vector.EY * 100, vector.EZ * 20, 1, 1, 1)
	let b = new Geometry.Cube(vector(-10.1, -10.2, -50), vector.EX * 20, vector.EY * 20, vector.EZ * 100, 1, 1, 1)
	Geometry c
	Geometry.and(a, b, c)
	del a
	del b
	
	model.paste_geometry(c, 0)