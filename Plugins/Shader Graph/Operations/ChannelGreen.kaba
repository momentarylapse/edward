use edward

class NodeChannelGreen extends shader.Node
	void __init__()
		super.__init__("ChannelGreen")
		params.add([shader.TYPE_COLOR, "color", "#ffffffff", ""])
		output.add([shader.TYPE_FLOAT, "green"])
	override const string code_pixel(out shader.BuilderContext c)
		var a = c.value(self, 0)
		var t = c.create_out(self, 0)
		return "
	float {{t}} = {{a}}.g;"
