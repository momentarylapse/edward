#include "api.kaba"

void main()
	data_model.BeginActionGroup()
	int n = data_model.Vertex.num
	data_model.AddVertex(vector(10,10,0), -1)
	data_model.AddVertex(vector(10,0,0), -1)
	data_model.AddVertex(vector(0,0,0), -1)
	data_model.AddTriangle(n + 0, n + 1, n + 2)
	data_model.EndActionGroup()
	