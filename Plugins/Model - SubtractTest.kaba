use "api.kaba"

void main()

	ModelGeometry *a = new ModelGeometryCube(vector(-50, -50, -10), e_x * 100, e_y * 100, e_z * 20, 1, 1, 1)
	ModelGeometry *b = new ModelGeometryCube(vector(-10, 0, -50), e_x * 20, e_y * 100, e_z * 100, 1, 1, 1)
	ModelGeometry c
	ModelGeometrySubtract(*a, *b, c)
	delete(a)
	delete(b)
	
	data_model.PasteGeometry(c, 0)