Dialog texture-scale-dialog "Scale texture" width=300 height=100
	Grid ? "" vertical
		Grid ? ""
			Label ? "Width"
			SpinButton width "" range=1:4096 expandx
			---|
			Label ? "Height"
			SpinButton height "" range=1:4096 expandx
		Grid ? "" button-bar
			Button cancel "Cancel"
			DefButton ok "Ok"